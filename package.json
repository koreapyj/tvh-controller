{
  "name": "tvh-controller-monorepo",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "dev": "pnpm --parallel -r dev"
  },
  "engines": {
    "node": ">=22"
  },
  "packageManager": "pnpm@9.15.1",
  "license": "AGPL-3.0-or-later"
}
